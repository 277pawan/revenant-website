# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          VITE_SITE_URL: https://revenant-verify-933e4.web.app
          VITE_APP_URL: https://revenant-cloud-web.web.app
          VITE_API_URL: https://revenant-api-171384186168.asia-south1.run.app
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_REVENANT_VERIFY_933E4 }}
          channelId: live
          projectId: revenant-verify-933e4
